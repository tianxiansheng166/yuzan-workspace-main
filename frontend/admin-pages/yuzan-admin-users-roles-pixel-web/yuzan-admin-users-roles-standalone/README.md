# 语赞心声｜用户角色与权限管理独立网页

- 直接打开 `index.html`。
- 页面未写入任何现有项目。
- Logo 只使用用户提供的图案部分，文字由 HTML 渲染。
- 顶部山景被裁成横向装饰带，并做降饱和、提亮、轻微虚化与渐隐，不为展示完整图片而破坏构图。
- 表格、筛选、角色树、权限矩阵、用户状态和弹窗全部由 HTML/CSS/JS 重建。
- `reference/` 包含参考图和原始说明，用于后续校准。
