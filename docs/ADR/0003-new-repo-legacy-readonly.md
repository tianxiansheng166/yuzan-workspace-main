# ADR-0003：新仓库与旧项目只读隔离

状态：Accepted

## 决策

`yuzan-next` 独立初始化，旧项目只作为归档、迁移输入、需求和视觉参考。

## 理由

旧项目的 JSON 存储、多 HTML、单文件服务、覆盖 CSS 和 DOM 修补会持续污染新架构。继续“渐进修旧”难以建立可靠边界。

## 后果

- 旧代码不直接复制；
- 内容和数据通过迁移脚本；
- 功能迁移以用户闭环优先，不按页面数量；
- 旧项目可继续作为比赛备用演示，但必须与新系统表述区分。
