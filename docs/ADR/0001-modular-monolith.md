# ADR-0001：采用模块化单体

状态：Accepted

## 决策

新系统使用一个 NestJS API 部署单元，内部按边界上下文分模块；异步 worker 独立进程。首期不使用微服务。

## 理由

- 团队规模和业务模型仍在形成；
- 事务、权限和调试比独立扩展更重要；
- 旧微服务目录并未形成可运行架构；
- 模块边界未来可按实测瓶颈拆分。

## 后果

- 必须禁止跨模块直接访问表；
- 模块公开应用服务/端口；
- 先建设可观测性和契约；
- 只有出现独立扩展、故障隔离或团队边界证据才拆分。
