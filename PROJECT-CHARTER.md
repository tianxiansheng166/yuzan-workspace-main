# 语赞心声项目章程：快速产品收敛模式

> 生效日期：2026-07-13
>
> 适用仓库：`yuzan-next`
>
> 当前模式：`RAPID_PRODUCT_CONVERGENCE`
>
> 总入口：`orchestration/operating-system/README.md`

## 1. 项目现在真正缺的是什么

项目已经不再缺“更多孤立页面”和“更多后端模块骨架”。当前事实是：

- 后端 V3.1 的 P0/P1 教学主链路已有可启动、可迁移、可鉴权的接口基线；
- 前端页面数量已经不少，但大量页面仍依赖 demo gateway、pending/unavailable 场景或本地示例数据；
- 前端视觉曾多次在“背景板 + 等宽卡片 + 大黑体”之间反复，缺少完整构图、真实内容密度和页面级艺术指导；
- 前后端没有持续以真实纵向链路联调，导致“后端存在、前端没接；前端存在、真实操作没跑”；
- 过去把大量时间耗在任务前置审查、无穷约束、返工和最终集中整合，产品轮廓形成过慢。

因此，当前阶段的主目标不是继续扩大功能面，而是：

> 把已有后端、已有前端、已有视觉素材和已有业务规则，快速收敛成一条能真实登录、真实操作、真实保存、真实反馈、可连续演示的产品主线。

## 2. 下一阶段唯一总目标

优先完成以下真实闭环：

```text
用户登录并选择学校
  → 教师创建课程/班级/任务
  → 学生看到今日任务并进入学习
  → 学生提交进度或作业
  → 教师查看提交并给出反馈
  → 学生看到反馈
  → 教师查看基础成长报告
```

测评功能作为第二条高优先级闭环同步推进：

```text
教师创建测评
  → 学生完成朗读/书面测评
  → 服务端保存响应
  → 生成 pending / available / unavailable 的真实状态
  → 教师查看结果与历史对比
```

## 3. 当前优先级

### P0：必须投入最多资源

1. 前端从 demo gateway 切换到真实 API；
2. 登录、会话、学校选择和角色导航真实可用；
3. 教师课程、班级、任务、学生今日任务、提交、反馈形成闭环；
4. 测评持久化从 P2 gap 变为可演示真实流程；
5. 两组关键页面形成高质量视觉母版：学生学习族、教师工作台族；
6. 持续集成，不等所有代理结束后再一次性整合。

### P1：P0 稳定后紧跟

- 报告页接真实 reporting API；
- 录音采集、离线保留和上传状态；
- 课程内容工作台；
- 弱网与错误状态；
- 视觉系统在完整页面中收敛，而不是只维护抽象 token。

### P2：暂时冻结扩张

- 社区、合作、志愿者、培训、研究治理、平台管理写入；
- 新增与主教学闭环无关的页面；
- 大规模重构；
- 为“看起来完整”而继续制造 demo 数据；
- 在产品轮廓形成前进行全面审计、全面测试和全面代码清理。

P2 不是删除，只是在当前快速收敛阶段不主动消耗主力资源。

## 4. 组织模型

### 总指挥：网页 ChatGPT

负责方向、任务拆解、冲突裁决、阶段复盘和下一批提示词，不直接占用大量编码资源。

### Codex 桌面 A：学生体验与高保真视觉

负责学生首页、今日任务、学习播放器、测评学生端、移动端重构和学生视觉母版。

### Codex 桌面 B：教师体验与高保真视觉

负责教师首页、课程/班级/任务、提交复核、报告和教师视觉母版。

### Codex CLI：共享前端基础与技术性改造

负责 API 客户端、会话、类型、状态适配、页面运行时问题、构建和局部测试；不与两个桌面 Codex 同时写同一页面文件。

### TRAE 1–5：纵向功能切片

分别负责身份壳、教师教学链、学生学习链、测评闭环、报告与持续集成辅助。每个任务使用独立 worktree。

### TRAE 滚动集成者

不是一直等待所有任务完成，而是持续消费小检查点。若只有 5 个 TRAE 槽位，第 5 槽位按固定时间窗在“功能开发”和“集成者”之间切换。

## 5. 工作目录原则

- Codex A/B 是额度接力关系时：串行操作同一个 `codex-active` 工作现场；
- Codex A/B 真正并行做不同页面族时：必须使用独立 worktree；
- 每个 TRAE 功能任务：独立 worktree；
- 集成者：独占 `integration-live` worktree；
- 共享契约、Prisma、根依赖、全局路由和设计 token：采用单写者；
- 所有工作树都基于同一 Git 仓库，不要求每次 push/pull。

## 6. 开发速度规则

当前阶段采用“检查点完成”，不采用“生产级完成”。

一个检查点只需满足：

1. 一个可解释的小功能闭环已经形成；
2. 相关页面或接口能启动；
3. 至少做一次与改动直接相关的局部验证；
4. 已知问题写入检查点，不强制立即修完；
5. 形成一个本地 WIP commit，供集成者消费；
6. 不伪造真实成功，不破坏租户和权限边界。

以下内容不再要求每个小任务都完成：

- 全仓库测试；
- 全尺寸截图；
- 全面安全复核；
- 完整性能审计；
- 长篇交接报告；
- PR 和远程推送；
- 无 Bug 才允许集成。

这些工作进入固定硬化窗口集中处理。

## 7. 集成节奏

集成采用事件驱动加固定窗口：

- 任一工作者完成一个可运行小闭环，即提交检查点；
- 集成者每 45–90 分钟或队列出现高依赖检查点时处理一次；
- 公共契约、数据库、依赖和路由变更优先处理；
- 不要求其他工作者暂停；
- 只有在集成共享事实时，相关工作者进入短暂同步窗口；
- 冲突记录到 blocked 队列，不阻塞其他不相关检查点。

## 8. 前端最低质量线

任何关键页面不能再以“背景 + 几张卡片 + 大标题”作为完成标准。

关键页面必须体现：

- 明确的视觉焦点和主任务；
- 至少三层信息层级；
- 非机械等宽卡片构图；
- 真实内容密度和真实状态；
- 页面间共享视觉 DNA，但不复制同一模板；
- 桌面和移动端分别重构；
- 一处有辨识度的交互机制，而不是统一轻微上浮；
- 真实 API 或明确 gap 状态，不以示例数据冒充完成。

详细标准见 `orchestration/operating-system/FRONTEND-QUALITY-BAR.md`。

## 9. 后端最低质量线

后端当前不追求继续铺满所有模块，而要求：

- P0 链路真实可调用；
- 前端所需 DTO 和错误状态稳定；
- 学校和资源范围校验不能退化；
- 重要写入有幂等或重复提交策略；
- gap 必须明确返回，不伪造成功；
- 每条当前接入的前端链路至少有一次真实联调。

## 10. 自动执行边界

AI 可以在分配的工作树内自主创建、修改、移动、删除项目文件，运行项目相关命令并创建本地 WIP commit。

AI 不得自主执行：

- 磁盘、分区、引导、用户账户和大范围注册表修改；
- 删除 worktree 根目录或项目外文件；
- `git reset --hard`、`git clean -fdx`、强制推送；
- 清空全部 Docker 容器、镜像或 volume；
- 删除生产或未备份数据库；
- 全局关闭安全软件或防火墙；
- 批量终止所有同名进程；
- 提交密钥和真实学生数据。

详细规则见 `orchestration/operating-system/SAFE-AUTONOMY.md`。

## 11. 两周内判断是否有效

快速收敛模式是否成功，不看文档数量，按以下事实判断：

- 至少一条教师—学生教学闭环使用真实 API 跑通；
- 至少一条测评链路从页面写入真实持久层；
- 两组关键页面达到可展示视觉质量；
- demo gateway 在目标闭环中明显减少；
- 每日都有小检查点进入 integration-live；
- 没有在最后堆积一批无法整合的大分支；
- Codex 资源主要用于视觉与复杂实现，而非重复审查；
- TRAE 资源主要用于功能接入、重复劳动、局部测试和持续集成。

## 12. 章程优先级

发生冲突时按以下顺序裁决：

1. 用户当前明确要求；
2. 本章程；
3. 当前波次计划；
4. 已冻结契约和数据库事实；
5. 各角色规范；
6. 旧任务板和历史流程。

旧流程仍作为工程资料保留，但不得继续用重型审查流程阻塞快速产品收敛。
