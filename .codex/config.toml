[features]
goals = true
multi_agent = true

# Concurrency is intentionally not fixed here. The control plane computes safe
# parallel work from the DAG, write sets, leases, and registered runtime resources.
