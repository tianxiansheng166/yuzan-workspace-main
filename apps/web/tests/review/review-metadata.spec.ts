import { readFileSync } from "node:fs";import { resolve } from "node:path";import { describe,expect,it } from "vitest";const pages=resolve(import.meta.dirname,"../../app/pages/teacher/review");const list=readFileSync(resolve(pages,"index.vue"),"utf8");const detail=readFileSync(resolve(pages,"[submissionId]/index.vue"),"utf8");const feedback=readFileSync(resolve(pages,"[submissionId]/feedback.vue"),"utf8");describe("review metadata",()=>{it("uses stable titles",()=>{expect(list).toContain("title:'提交复核｜教师工作台'");expect(detail).toContain('title: "提交详情｜语赞心声"');expect(feedback).toContain('title: "教师反馈｜语赞心声"');});it("preserves dynamic routes",()=>{expect(detail).toContain("route.params.submissionId");expect(feedback).toContain("route.params.submissionId");});});