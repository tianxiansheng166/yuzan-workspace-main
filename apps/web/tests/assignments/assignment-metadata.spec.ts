import { readFileSync } from "node:fs";import { resolve } from "node:path";import { describe,expect,it } from "vitest";const pages=resolve(import.meta.dirname,"../../app/pages/teacher/assignments");const list=readFileSync(resolve(pages,"index.vue"),"utf8");const create=readFileSync(resolve(pages,"new.vue"),"utf8");const detail=readFileSync(resolve(pages,"[assignmentId]/index.vue"),"utf8");describe("assignment metadata",()=>{it("uses stable titles",()=>{expect(list).toContain('title: "作业｜教师工作台"');expect(create).toContain('title: "创建作业｜教师工作台"');expect(detail).toContain('title: "任务详情｜语赞心声"');});it("preserves the assignment route",()=>expect(detail).toContain("route.params.assignmentId"));});