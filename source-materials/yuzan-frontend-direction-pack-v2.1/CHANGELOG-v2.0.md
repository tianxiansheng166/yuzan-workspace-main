# v2.0 更新

相较 v1.0：

- 新增 10 张独立视觉参考图；
- 新增视觉参考的可编辑 HTML/CSS 结构源；
- 新增 20 份页面、背景、按钮、组件、封面、图标和动效 Prompt；
- 新增图片在 Nuxt/Vue 中的使用方式；
- 新增四路前端并发开发合同；
- 新增上下文压缩规则；
- 新增前端与既有后端/API 任务的合并规则；
- 新增唯一 integration controller 工作流；
- 新增 forbidden pattern scan；
- 前端审查改为“各流自检 + 一次集中审查 + 一次集中返工”。
