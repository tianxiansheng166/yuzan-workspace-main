# 系统运维｜服务提供商管理

独立静态网页，未写入任何现有项目。

## 打开
直接打开 `index.html`，或运行 `python -m http.server 8080`。

## 资产使用
- Logo 仅提取用户提供的红橙色图案，品牌文字由 HTML 渲染。
- 服务分类底部山景由用户提供的横向山水图重新裁切、降饱和和渐隐，不为展示整张图片而破坏构图。
- 拓扑区域纹理由用户提供的路径山川图提取低对比地形线，作为背景装饰。
- 所有服务名称、状态、按钮、密钥状态、测试结果均为 HTML/CSS/JS，不写入图片。
