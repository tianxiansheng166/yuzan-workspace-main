(()=>{const toast=document.getElementById('toast');let tt;const say=m=>{toast.textContent=m;toast.classList.add('show');clearTimeout(tt);tt=setTimeout(()=>toast.classList.remove('show'),1600)};const dialog=document.getElementById('partnerDialog');document.querySelectorAll('[data-action="partner"]').forEach(b=>b.addEventListener('click',()=>dialog.showModal()));document.querySelectorAll('.dialog-options button').forEach(b=>b.addEventListener('click',e=>{e.preventDefault();document.querySelectorAll('.dialog-options button').forEach(x=>x.classList.remove('selected'));b.classList.add('selected')}));dialog.addEventListener('close',()=>{if(dialog.returnValue==='submit')say('合作意向已记录（演示）')});document.querySelectorAll('.wing-title').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.wing').forEach(w=>w.classList.remove('focus'));btn.closest('.wing').classList.add('focus');say('已聚焦'+btn.textContent+'服务路径')}));document.querySelectorAll('.nav-item').forEach(a=>a.addEventListener('click',e=>{document.querySelectorAll('.nav-item').forEach(n=>n.classList.remove('active'));a.classList.add('active')}));const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;if(!reduce){document.querySelectorAll('.wing').forEach((w,i)=>{w.animate([{opacity:.2,transform:'translateY(12px)'},{opacity:1,transform:'translateY(0)'}],{duration:520,delay:120*i,fill:'both',easing:'ease-out'})})}})();