# Design Lab

这里管理 Codex 多模态视觉探索和最终资产，不是随意堆生成图片的目录。

流程：

1. 从 `asset-briefs/` 复制 brief；
2. 明确页面任务、构图位置、裁切、体积和禁用项；
3. 生成多个方向；
4. 产品/设计/内容审查；
5. 选中后人工修正、压缩和响应式导出；
6. 在 `asset-register.csv` 登记；
7. 将生产资产放到 `yuzan-next/apps/web/public/art/` 或 UI icons；
8. 在 `reviews/` 保存审查记录。

任何未登记资产不得合并。
