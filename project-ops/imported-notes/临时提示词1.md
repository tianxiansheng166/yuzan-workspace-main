你现在担任“语赞心声项目 GitHub 仓库迁移总控”。

本轮目标：

1. 保留现有完整 Git 历史；
2. 将当前项目推送到新的 GitHub 私有仓库；
3. 将安全的提示词、报告和协作记录纳入版本管理；
4. 不上传本机 worktree、环境变量、旧项目原始数据或缓存；
5. 建立物理机和虚拟机都能理解的项目状态中心；
6. 不修改 GOV-002、MIG-001 的业务内容。

远程仓库：

<git@github.com>:tianxiansheng166/yuzan-workspace-main.git

真正的 Git 仓库：

/home/admin01/Documents/yuzan-workspace-main/yuzan-next

外层协作目录：

/home/admin01/Documents/yuzan-workspace-main

当前预期状态：

main HEAD：
79971885933911630f651a84f90c7655759f7898

已合并：

- GOV-001
- GOV-003
- GOV-004

尚未合并：

- GOV-002
  branch: task/gov-002-contract
  current commit: 2496f59e00f735372af403498d8b831d1169c3c1
  状态：REBASED / NOT\_MERGED / 等待最终契约修订
- MIG-001
  状态需要从实际分支和工作树确认，不得猜测

严禁：

- 删除任何分支；
- 删除任何 worktree；
- force push；
- 重写已有提交；
- 移动 .git；
- 在外层目录重新 git init；
- 上传 .env；
- 上传 SSH 密钥；
- 上传 legacy/source-tree；
- 上传数据库或 MinIO 数据；
- 上传 node\_modules、.nuxt、dist 或缓存；
- 修改 OpenAPI、Prisma、UI 或业务代码；
- 把 GOV-002 标记为已合并。

━━━━━━━━━━━━━━━━━━━━
一、检查当前仓库
━━━━━━━━━━━━━━━━━━━━

执行：

cd /home/admin01/Documents/yuzan-workspace-main/yuzan-next

pwd
git rev-parse --show-toplevel
git branch --show-current
git status --short
git rev-parse HEAD
git remote -v
git branch -vv
git worktree list
git log -8 --oneline --decorate

要求：

- Git 根目录是 yuzan-next；
- 当前分支是 main；
- status 为空；
- HEAD 是 79971885933911630f651a84f90c7655759f7898。

不符合时停止并返回：

REPOSITORY\_BOOTSTRAP\_BLOCKED\_STATE

不要自动修正未知状态。

━━━━━━━━━━━━━━━━━━━━
二、建立完整 Git 备份
━━━━━━━━━━━━━━━━━━━━

执行：

mkdir -p /home/admin01/Backups/yuzan-git

git bundle create \
/home/admin01/Backups/yuzan-git/yuzan-before-github-20260709.bundle \
\--all

git bundle verify \
/home/admin01/Backups/yuzan-git/yuzan-before-github-20260709.bundle

保存清单：

git branch -vv \\

> /home/admin01/Backups/yuzan-git/branches-before-github.txt

git worktree list \\

> /home/admin01/Backups/yuzan-git/worktrees-before-github.txt

git log --all --decorate --oneline \\

> /home/admin01/Backups/yuzan-git/history-before-github.txt

这些备份文件不得加入项目仓库。

━━━━━━━━━━━━━━━━━━━━
三、审计现有 Git 内容
━━━━━━━━━━━━━━━━━━━━

检查是否提交了环境文件：

git ls-files | grep -E '(^|/).env($|.)' || true
git log --all --name-only --pretty=format: -- .env '.env.\*' \
\| sort -u

检查密钥文件：

git ls-files | grep -Ei \
'(^|/)(id\_rsa|id\_ed25519)$|.pem$|.p12$|.key$|credentials|secrets' \
|| true

检查常见敏感内容，逐个本地分支扫描：

for ref in $(git for-each-ref \
\--format="%(refname)" refs/heads); do
echo "===== $ref ====="
git grep -n -I -E \
'-----BEGIN (RSA |OPENSSH |EC )?PRIVATE KEY-----|ghp\_\[A-Za-z0-9]+|github\_pat\_\[A-Za-z0-9\_]+|AKIA\[0-9A-Z]{16}|DATABASE\_URL=.\*://\[^\[:space:]]+:\[^\[:space:]@]+@' \
"$ref" -- . \
':!pnpm-lock.yaml' \
|| true
done

所有命中都必须人工判断。

发现真实密钥、密码、Token 或私钥时：

- 不得 push；
- 返回 REPOSITORY\_BOOTSTRAP\_BLOCKED\_SECRET；
- 列出文件和类型；
- 不要在报告中打印完整密钥值。

检查超大 Git 对象：

git rev-list --objects --all \
\| git cat-file \
\--batch-check='%(objecttype) %(objectname) %(objectsize) %(rest)' \
\| awk '$1=="blob" && $3>=52428800 {
printf "%.2f MiB\t%s\n", $3/1048576, $4
}' \
\| sort -nr

列出 50 MiB 以上对象。

存在超过 100 MiB 的对象时停止，不得 push。

━━━━━━━━━━━━━━━━━━━━
四、检查远程仓库
━━━━━━━━━━━━━━━━━━━━

测试 SSH：

ssh -T <git@github.com> || true

GitHub 显示 authenticated 且不提供 shell access 属于正常认证结果。

检查目标远程：

git ls-remote \
<git@github.com>:tianxiansheng166/yuzan-workspace-main.git

如果远程为空，可以继续。

如果远程已经有 refs：

- 列出 refs；
- 比较是否属于当前项目；
- 只要存在无法确认的远程历史，就停止；
- 返回 REPOSITORY\_BOOTSTRAP\_BLOCKED\_REMOTE\_NOT\_EMPTY；
- 不得 force push 或覆盖远程。

检查当前 origin：

git remote -v

如果没有 origin：

git remote add origin \
<git@github.com>:tianxiansheng166/yuzan-workspace-main.git

如果 origin 已经指向同一个地址：

保持不变。

如果 origin 指向其他地址：

不得删除旧 remote。

执行：

git remote rename origin previous-origin

然后：

git remote add origin \
<git@github.com>:tianxiansheng166/yuzan-workspace-main.git

最终确认：

git remote -v

━━━━━━━━━━━━━━━━━━━━
五、盘点需要版本化的协作资料
━━━━━━━━━━━━━━━━━━━━

只读检查以下位置：

/home/admin01/Documents/yuzan-workspace-main/runtime-prompts
/home/admin01/Documents/yuzan-workspace-main/runtime-reports
/home/admin01/Documents/yuzan-workspace-main/orchestration
/home/admin01/Documents/yuzan-workspace-main/_.md
/home/admin01/Documents/yuzan-workspace-main/_.txt

同时检查当前仓库内已有：

orchestration/\*\*
docs/\*\*
design-lab/\*\*
legacy/exports/\*\*
legacy/review/\*\*
legacy/reports/\*\*

建立文件清单，分别分类：

A. 必须纳入仓库

- 正式任务提示词
- 审查提示词
- 最终交接报告
- 架构决策
- 协作协议
- 任务状态
- 迁移审计报告
- 安全的文本记录

B. 可纳入但需脱敏

- 带本机绝对路径的报告
- 含邮箱、电话或个人姓名的记录
- 包含旧项目片段的报告
- 临时提示词文件

C. 禁止纳入

- worktrees/\*\*
- legacy/source-tree/\*\*
- .env
- node\_modules
- .nuxt
- dist
- coverage
- 数据库文件
- MinIO 文件
- 二进制备份
- bundle
- SSH 密钥
- \~/.trae-cn
- Codex/Trae 本地缓存
- /tmp 文件
- 会话内部数据库

不得复制整个外层目录。

━━━━━━━━━━━━━━━━━━━━
六、创建统一项目运营目录
━━━━━━━━━━━━━━━━━━━━

进入：

cd /home/admin01/Documents/yuzan-workspace-main/yuzan-next

创建分支：

git switch -c chore/repository-bootstrap-20260709

创建：

mkdir -p \
project-ops/runtime-prompts \
project-ops/runtime-reports \
project-ops/imported-notes \
project-ops/decisions \
project-ops/handoffs

安全复制正式提示词：

如果目录存在：

rsync -a \
\--exclude '_.log'_ _\
\--exclude '_.tmp' \
\--exclude '.DS\_Store' \
/home/admin01/Documents/yuzan-workspace-main/runtime-prompts/ \
project-ops/runtime-prompts/

安全复制正式报告：

如果目录存在：

rsync -a \
\--exclude '_.log'_ _\
\--exclude '_.tmp' \
\--exclude '.DS\_Store' \
/home/admin01/Documents/yuzan-workspace-main/runtime-reports/ \
project-ops/runtime-reports/

检查外层根目录的 md/txt 文件。

只将确认属于项目提示词、协作说明或审查材料的文件复制到：

project-ops/imported-notes/

不得复制：

- 密码说明；
- SSH 私钥；
- 环境配置；
- 个人聊天；
- 无关文件；
- 原始旧项目数据。

复制前后都要执行敏感信息扫描。

━━━━━━━━━━━━━━━━━━━━
七、生成项目真源文档
━━━━━━━━━━━━━━━━━━━━

创建：

project-ops/README.md

内容必须说明：

- project-ops 是多机器、多 AI 协作真源；
- 代码事实以 main 为准；
- 任务状态以 TASK-REGISTRY.yaml 为准；
- 分支状态以 Git 为准；
- 不以某个聊天窗口的描述为最终事实。

创建：

project-ops/CURRENT-STATE.md

必须根据实际 Git 数据记录：

- 当前 main HEAD；
- 已合并任务；
- 未合并任务；
- 各任务当前 branch；
- 当前 commit；
- 当前状态；
- 已知验证结果；
- 已知基线问题。

当前必须明确写：

GOV-001：MERGED
GOV-003：MERGED
GOV-004：MERGED
GOV-002：REBASED\_NOT\_MERGED
GOV-002 commit：
2496f59e00f735372af403498d8b831d1169c3c1

MIG-001 状态必须通过实际分支检查填写，不得猜测。

创建：

project-ops/TASK-REGISTRY.yaml

至少包括：

version
updatedAt
mainHead
tasks:

- id
- status
- ownerMachine
- ownerAgent
- branch
- commit
- baseCommit
- allowedPaths
- dependencies
- verification
- reportPath

创建：

project-ops/BRANCH-INVENTORY.md

从以下命令生成并解释：

git branch -vv
git worktree list

创建：

project-ops/MACHINE-REGISTRY.md

至少定义：

VM-UBUNTU

- 角色：当前集成总控
- main 操作者：是
- Codex：现有任务
- Trae：现有任务

PHYSICAL-UBUNTU

- 角色：新增开发节点
- main 操作者：否
- Codex：待分配
- Trae：待分配

创建：

project-ops/MULTI-MACHINE-PLAYBOOK.md

必须明确：

1. GitHub 是跨机器唯一交换中心；
2. 每台机器独立 clone；
3. 每台机器独立创建 worktree；
4. 不复制虚拟机 worktree 到物理机；
5. 一个任务只能有一个 owner；
6. 一个分支同一时间只能由一台机器写；
7. 开发 AI 不直接操作 main；
8. main 只由 VM 集成总控合并；
9. 所有任务完成后 push；
10. 远程分支已被其他机器使用后不得普通 force push；
11. 修改审查优先新 commit；
12. 最终集成前 rebase 最新 main；
13. 验证通过后 fast-forward 合并。

创建：

project-ops/AI-TASK-TEMPLATE.md

固定字段：

- Task ID
- Owner machine
- Owner agent
- Base commit
- Branch
- Worktree
- Allowed paths
- Forbidden paths
- Dependencies
- Goal
- Required evidence
- Validation commands
- Commit policy
- Push policy
- Handoff format

创建：

project-ops/UPLOAD-MANIFEST.md

分别列出：

- 已纳入的目录；
- 已排除的目录；
- 排除原因；
- 隐私扫描结论；
- 版权风险结论；
- 大文件检查结论。

━━━━━━━━━━━━━━━━━━━━
八、更新 .gitignore
━━━━━━━━━━━━━━━━━━━━

仅在确有必要时补充以下本地内容：

project-ops/local/
\*.bundle
\*.local.log
_.local.json
runtime-local/
.env
.env._
!.env.example

不要重复已有规则。

不要忽略正式的：

project-ops/runtime-prompts
project-ops/runtime-reports

━━━━━━━━━━━━━━━━━━━━
九、扫描导入后的资料
━━━━━━━━━━━━━━━━━━━━

执行：

git status --short

扫描 project-ops：

rg -n -I \
'-----BEGIN (RSA |OPENSSH |EC )?PRIVATE KEY-----|ghp\_\[A-Za-z0-9]+|github\_pat\_\[A-Za-z0-9\_]+|AKIA\[0-9A-Z]{16}' \
project-ops \
|| true

扫描常见个人数据：

rg -n -I \
'1\[3-9]\[0-9]{9}|\[A-Za-z0-9.\_%+-]+@\[A-Za-z0-9.-]+.\[A-Za-z]{2,}|\[0-9]{17}\[0-9Xx]' \
project-ops \
|| true

每个命中必须审查。

正式任务 commit hash、GitHub 用户名、项目占位邮箱不自动视为隐私泄漏。

真实手机号、个人邮箱、身份证、密钥必须脱敏或排除。

检查绝对路径：

rg -n \
'/home/admin01|C:\\|file://' \
project-ops \
|| true

绝对路径可以保留在历史运行报告中，但必须在 UPLOAD-MANIFEST.md 说明：

- 它们仅是历史执行路径；
- 物理机不得直接复用；
- 新任务通过仓库根目录动态解析路径。

━━━━━━━━━━━━━━━━━━━━
十、验证并提交项目运营资料
━━━━━━━━━━━━━━━━━━━━

执行：

git status --short
git diff --check
git diff --stat
git diff --name-status

只允许：

project-ops/\*\*
.gitignore（确有必要时）

不得修改产品代码、OpenAPI、Prisma、UI 或根依赖。

执行：

source \~/.nvm/nvm.sh
nvm use 24

pnpm install --frozen-lockfile
pnpm format:check
pnpm typecheck
pnpm build

记录：

set +e
pnpm test > /tmp/repository-bootstrap-test.log 2>&1
TEST\_EXIT=$?
set -e

当前 main 尚未合并 GOV-002 时，允许 test 只因 OpenAPI 基线问题失败。

执行：

git add project-ops

如修改 .gitignore：

git add .gitignore

提交：

git commit -m \
"chore(ops): establish multi-machine collaboration source of truth"

记录 commit。

━━━━━━━━━━━━━━━━━━━━
十一、首次推送当前 Git 历史
━━━━━━━━━━━━━━━━━━━━

先回到 main，但不要丢失 bootstrap 分支：

git switch main

再次检查：

git status --short
git rev-parse HEAD

推送当前 main：

git push -u origin main

创建快照标签：

git tag -a snapshot/pre-multimachine-20260709 \
79971885933911630f651a84f90c7655759f7898 \
-m "Stable main before multi-machine collaboration"

git push origin snapshot/pre-multimachine-20260709

列出全部 task 分支：

git for-each-ref \
\--format='%(refname:short) %(objectname:short)' \
refs/heads/task/

逐个推送所有已确认属于本项目的 task 分支：

for branch in $(git for-each-ref \
\--format='%(refname:short)' refs/heads/task/); do
git push -u origin "$branch"
done

不得使用 force。

推送后确认：

git ls-remote --heads origin
git ls-remote --tags origin

━━━━━━━━━━━━━━━━━━━━
十二、合并项目运营资料
━━━━━━━━━━━━━━━━━━━━

回到 bootstrap 分支：

git switch chore/repository-bootstrap-20260709

确认干净：

git status --short

如 main 在此期间未变化，继续。

如 main 发生变化：

git rebase main

冲突时中止，不得自行解决未知冲突。

验证后回到 main：

git switch main

执行 fast-forward：

git merge --ff-only chore/repository-bootstrap-20260709

推送：

git push origin main

推送 bootstrap 分支作为审计记录：

git push -u origin chore/repository-bootstrap-20260709

最终：

git status --short
git rev-parse HEAD
git log -8 --oneline --decorate
git remote -v
git branch -vv

━━━━━━━━━━━━━━━━━━━━
十三、最终报告
━━━━━━━━━━━━━━━━━━━━

返回：

状态：REPOSITORY\_BOOTSTRAP\_COMPLETE 或 BLOCKED
Git 根目录：
远程仓库：
原 main HEAD：
最终 main HEAD：
bootstrap commit：
快照 tag：

远程 main：
远程 task 分支：
远程 tags：

导入提示词数量：
导入报告数量：
导入历史说明数量：

已纳入目录：
已排除目录：
敏感信息扫描：
个人数据扫描：
大文件扫描：
是否发现超过 100 MiB 文件：

是否提交 .env：
是否上传 legacy/source-tree：
是否上传 worktrees：
是否上传数据库/MinIO 数据：

GOV-002 状态：
GOV-002 commit：
GOV-002 是否已合并：
MIG-001 状态：

git status：
下一步物理机 clone 所需 main commit：

完成后停止，不处理 GOV-002，不处理 MIG-001，不创建物理机任务。
