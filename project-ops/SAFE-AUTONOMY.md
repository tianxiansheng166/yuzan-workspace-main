# 安全自主执行边界

允许在任务 worktree 和 `allowed_paths` 内修改项目文件、运行局部验证并创建提交。

禁止未经明确授权执行：

- `git reset --hard`、`git clean -fdx` 或强制推送；
- 删除未备份数据库、Docker volume 或其他任务未提交工作；
- 修改任务白名单外的共享文件；
- 提交密钥、真实学生数据和来源不明资产；
- 批量终止无关进程或删除项目外目录。

遇到契约、schema、权限或数据解释冲突时停止并提交变更请求。
